<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: #4a90e2;
      color: white;
      padding: 20px;
      border-radius: 5px 5px 0 0;
    }
    .content {
      background-color: #f9f9f9;
      padding: 20px;
      border: 1px solid #ddd;
      border-top: none;
    }
    .field {
      margin-bottom: 15px;
    }
    .label {
      font-weight: bold;
      color: #555;
    }
    .footer {
      margin-top: 20px;
      padding-top: 20px;
      border-top: 1px solid #ddd;
      font-size: 12px;
      color: #888;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="header">
    <h2>New Contact Form Submission</h2>
  </div>
  
  <div class="content">
    <div class="field">
      <span class="label">From:</span>
      <%= @contact.name %>
    </div>
    
    <div class="field">
      <span class="label">Email:</span>
      <a href="mailto:<%= @contact.email %>"><%= @contact.email %></a>
    </div>
    
    <% if @contact.subject.present? %>
      <div class="field">
        <span class="label">Subject:</span>
        <%= @contact.subject %>
      </div>
    <% end %>
    
    <div class="field">
      <span class="label">Message:</span>
      <p style="white-space: pre-wrap;"><%= @contact.message %></p>
    </div>
    
    <div class="field">
      <span class="label">Submitted:</span>
      <%= @contact.created_at.strftime('%B %d, %Y at %I:%M %p') %>
    </div>
  </div>
  
  <div class="footer">
    <p>This email was sent from your portfolio contact form at justinnegron.dev</p>
  </div>
</body>
</html>